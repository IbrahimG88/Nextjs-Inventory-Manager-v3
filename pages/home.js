import { Fragment } from "react";

export default function Home() {
  return (
    <Fragment>
      <p>HomePage</p>
    </Fragment>
  );
}

// here first add the update tests list component in return
// optimized consumption ,odule component and add corresponding api and helper db utils

// add menu and navbar
